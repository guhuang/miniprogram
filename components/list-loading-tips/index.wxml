<view class="loading-wrap {{!isEnd && 'icon-loading'}}" wx:if="{{!isEmpty}}">
  <block wx:if="{{!isEnd}}">
    {{ loadingTips }}
    <icon-rotate />
  </block>
  <block wx:else>
    {{ endTips }}
  </block>
</view>
<view class="empty-wrap" wx:else>
  这里似乎没有东西呢
</view>